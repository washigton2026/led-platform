global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: lumyx
    static_configs:
      # led_hal::serve_metrics(emitters, "0.0.0.0:9464") on the show machine
      - targets: ["host.docker.internal:9464"]
