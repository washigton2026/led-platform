[package]
name = "led-hal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Hardware Abstraction Layer — the single boundary between the Core Engine and physical devices."

[dependencies]
led-core = { path = "../led-core" }
