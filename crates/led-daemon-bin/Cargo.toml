[package]
name = "led-daemon-bin"
version = "0.1.0"
edition = "2021"

# O PROCESSO. A maquina de estados vive no `led-daemon`, que continua sem dependencias
# (ADR-0023). Separar os dois e o que impede o relogio, o I/O e o CLI de entrarem no crate
# que define o contrato.
[dependencies]
led-daemon = { path = "../led-daemon" }
led-show-recorder = { path = "../led-show-recorder" }
# GS4.1 — a saida reusa os drivers ja validados em hardware; nenhuma segunda
# serializacao de protocolo entra no projeto.
led-core = { path = "../led-core" }
led-hal = { path = "../led-hal" }
led-protocols = { path = "../led-protocols" }
led-player = { path = "../led-player" }
led-hardware-profile = { path = "../led-hardware-profile" }

[dev-dependencies]
# Só nos testes do `loader`, para escrever um .lumyx de fixture.
led-core = { path = "../led-core" }
led-show-recorder = { path = "../led-show-recorder" }

[lib]
name = "led_daemon_bin"
path = "src/lib.rs"

[[bin]]
name = "led-daemon"
path = "src/main.rs"
