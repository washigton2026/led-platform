[package]
name = "led-triple"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "The wait-free triple buffer for the render→send handoff. Leaf: std-only, zero dependencies."

# Sem dependências, de propósito. O triple buffer é o único sítio `unsafe` desta
# fatia do caminho, e um leaf sem dependências é o que permite pô-lo sob Miri
# isolado do resto do workspace (ver a lista de crates em `lumyx-e2e.sh`).
[dependencies]
